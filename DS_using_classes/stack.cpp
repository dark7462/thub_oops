#include <bits/stdc++.h>
#include "stack.h"
using namespace std;
#define ll long long

int main(){
    // your code goes here
    Stack<int> st;
    st.pop();
    st.push(100);
    st.push(200);
    st.push(300);
    st.display();
    return 0;
}